<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rmutl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rmutl">
        <activity android:name="neua_th.qcar.rmutl.listproduct"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name="neua_th.qcar.rmutl.selectcarmember"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name="neua_th.qcar.rmutl.QueueActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="neua_th.qcar.rmutl.history"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="neua_th.qcar.rmutl.contact"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="neua_th.qcar.rmutl.hindlogo"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="neua_th.qcar.rmutl.profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="neua_th.qcar.rmutl.Menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="neua_th.qcar.rmutl.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>